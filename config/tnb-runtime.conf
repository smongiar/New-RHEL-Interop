# tmpfiles.d configuration for TNB user runtime directory
# Note: The actual UID-specific directory (/run/user/UID) is created by
# the podman-tnbuser.service at startup to handle dynamic UIDs
# This file just ensures the parent /run/user directory exists
d /run/user 0755 root root -
